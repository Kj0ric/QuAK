# Smoke test: single-state parent invoking a single-state child.
# Child 1 loops on 'a' with weight 1 and immediately accepts (initial = final).
# Every invocation returns Max_f = 1; isNonEmpty(LimSup, Max_f, 1) should be TRUE.

@PARENT
a : 1, p0 -> p0

@CHILD 0

@CHILD 1
final: c0
a : 1, c0 -> c0
